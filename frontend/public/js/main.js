/**
 * main.js — Landing Page Initializer
 * Entry point: ensures all modules run after DOMContentLoaded.
 * navbar.js and animations.js are loaded as separate scripts
 * and wrap themselves in IIFEs, but this file can be used
 * for any future page-level setup or global utilities.
 */

document.addEventListener('DOMContentLoaded', function () {
    // navbar.js and animations.js self-initialize via their own IIFEs.
    // Add any landing-page-wide initialization below if needed in future.
});
